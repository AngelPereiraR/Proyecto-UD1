# ==================== MONGODB ATLAS CONFIGURATION ====================
# Reemplaza los siguientes valores con tu información real de MongoDB Atlas

# Connection String de MongoDB Atlas
# Formato: mongodb+srv://usuario:password@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://smartparking_user:TU_PASSWORD_AQUI@smartparkingcluster.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=SmartParkingCluster

# Nombre de la base de datos
MONGO_DB=smartparking

# ==================== FLASK CONFIGURATION ====================

# Entorno de Flask (development o production)
FLASK_ENV=development

# Modo debug (True o False)
FLASK_DEBUG=True

# Clave secreta para sesiones (genera una clave aleatoria segura)
SECRET_KEY=tu_clave_secreta_super_segura_cambiala_en_produccion

# ==================== NOTAS ====================
# 1. Obtén tu MONGO_URI desde MongoDB Atlas:
#    - Ve a tu cluster → Connect → Drivers
#    - Copia la connection string
#    - Reemplaza <password> con tu contraseña real
#
# 2. Para generar una SECRET_KEY segura en Python:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 3. NUNCA subas este archivo a Git con credenciales reales
#    Añade .env a tu .gitignore
